---
all:
  children:
    workstations:
      hosts:
        home-desktop:
        matebook:
    home-servers:
      hosts:
        home-media:
        home-edge:
    docker-swarm-members:
      hosts:
        home-edge:
        home-media:
        home-desktop:
    docker-swarm-managers:
      hosts:
        home-edge:
    docker-swarm-workers:
      hosts:
        home-media:
    public-servers:
      hosts:
        frrcode-prod:
