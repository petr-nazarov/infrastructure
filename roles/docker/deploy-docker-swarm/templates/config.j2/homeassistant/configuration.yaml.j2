{# Managed by Ansible: edits made on the host are overwritten on deploy.
   This is a Jinja template, so wrap any Home Assistant template (double
   braces) in raw / endraw blocks. #}
default_config:

http:
  use_x_forwarded_for: true
  trusted_proxies:
    - 172.18.0.0/16

automation: !include automations.yaml
script: !include scripts.yaml
scene: !include scenes.yaml
climate:
  - platform: smartir
    name: AC test 1522
    unique_id: ac_test_1522
    device_code: 1522
    controller_data:
      controller_type: Broadlink
      remote_entity: remote.universal_remote
    temperature_sensor: sensor.universal_remote_temperature
    humidity_sensor: sensor.universal_remote_humidity
