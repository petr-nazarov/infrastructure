---
# The mac is its own controller, so it is managed over a local connection.
# Secrets (ansible_become_password) live next to this in macbook.sops.yml.
ansible_connection: local
ansible_python_interpreter: "{{ ansible_playbook_python }}"
